import Layout from '../layouts/Layout';

export default function Entry() {
  return (
    <div>
      <Layout>
        <p>This component will allow users to log in.</p>
      </Layout>
    </div>
  );
}