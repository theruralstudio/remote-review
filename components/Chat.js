import Link from 'next/link';

const linkStyle = {
  marginRight: 15
};

const Chat = () => (
  <div>
    <p>Chat functionality will go here.</p>
    <p>The chat is disabled until you log in.</p>
  </div>
);

export default Chat;